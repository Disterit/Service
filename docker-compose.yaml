version: "3.8"
services:
  db:
    image: postgres:17
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=dister
    ports:
      - 5438:5432
    volumes:
      - ./grpc/migrations:/docker-entrypoint-initdb.d